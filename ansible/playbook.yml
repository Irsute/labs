---
- hosts: all
  become: yes
  become_user: root
  gather_facts: true

  tasks:
    - name: configure timezone
      timezone: 
        name: Europe/Paris
    - name: install ansible
      apt:
        name:  ansible
        state: latest
      when: "'ansible-host' in inventory_hostname"
